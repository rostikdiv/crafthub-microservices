spring:
  cloud:
    discovery:
      enabled: false
  datasource:
    hikari:
      maximum-pool-size: 2
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

application:
  config:
    order-url: ${ORDER_SERVICE_URL}
