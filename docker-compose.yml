version: '3.8'

services:
  # 1. Service Discovery
  service-discovery:
    build:
      context: .
      dockerfile: ./service-discovery/Dockerfile
    container_name: service-discovery
    ports:
      - "8761:8761"
    environment:
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net

  # 2. API Gateway
  api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    container_name: api-gateway
    ports:
      - "8080:8080"
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - CORS_ALLOWED_ORIGINS=http://localhost:5173,https://rachell-diaphanometric-viva.ngrok-free.app,https://rachell-diaphanometric-viva.ngrok-free.dev
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery

  # 3. User Service DB
  postgres-user-db:
    image: postgres:16.2-alpine
    container_name: postgres-user-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=user_service_db
    volumes:
      - postgres-user-data:/var/lib/postgresql/data
    networks:
      - milhub-net
    ports:
      - "5431:5432"

  # 4. Product Service DB
  postgres-product-db:
    image: postgres:16.2-alpine
    container_name: postgres-product-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=product_service_db
    volumes:
      - postgres-product-data:/var/lib/postgresql/data
    networks:
      - milhub-net
    ports:
      - "5434:5432"

  # 5. Order Service DB
  postgres-order-db:
    image: postgres:16.2-alpine
    container_name: postgres-order-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=order_service_db
    volumes:
      - postgres-order-data:/var/lib/postgresql/data
    networks:
      - milhub-net
    ports:
      - "5433:5432"

  # Payment Service DB
  postgres-payment-db:
    image: postgres:16.2-alpine
    container_name: postgres-payment-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=payment_service_db
    volumes:
      - postgres-payment-data:/var/lib/postgresql/data
    networks:
      - milhub-net
    ports:
      - "5435:5432"

  # Delivery Service DB
  postgres-delivery-db:
    image: postgres:16.2-alpine
    container_name: postgres-delivery-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=delivery_service_db
    volumes:
      - postgres-delivery-data:/var/lib/postgresql/data
    networks:
      - milhub-net
    ports:
      - "5436:5432"

  # 6. User Service
  user-service:
    build:
      context: .
      dockerfile: ./user-service/Dockerfile
    container_name: user-service
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - DB_URL=jdbc:postgresql://postgres-user-db:5432/user_service_db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
      - ADMIN_EMAIL=${ADMIN_EMAIL:-admin@milhub.ua}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-Password123!}
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - postgres-user-db

  # 7. Product Service
  product-service:
    build:
      context: .
      dockerfile: ./product-service/Dockerfile
    container_name: product-service
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - DB_URL=jdbc:postgresql://postgres-product-db:5432/product_service_db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - REDIS_HOST=redis
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - postgres-product-db

  # 8. Order Service
  order-service:
    build:
      context: .
      dockerfile: ./order-service/Dockerfile
    container_name: order-service
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - DB_URL=jdbc:postgresql://postgres-order-db:5432/order_service_db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - PRODUCT_SERVICE_URL=http://product-service/api/v1/products
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - postgres-order-db
      - product-service

  kafka:
    image: apache/kafka:3.7.0
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 1
      KAFKA_HEAP_OPTS: "-Xmx512m -Xms512m"
    networks:
      - milhub-net

  # 11. Notification Service
  notification-service:
    build:
      context: .
      dockerfile: ./notification-service/Dockerfile
    container_name: notification-service
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
      - MAILTRAP_USER=${MAILTRAP_USER}
      - MAILTRAP_PASS=${MAILTRAP_PASS}
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - kafka

  # 12. Cart Service
  cart-service:
    build:
      context: .
      dockerfile: ./cart-service/Dockerfile
    container_name: cart-service
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - MONGO_HOST=mongo
      - MONGO_PORT=27017
      - MONGO_DB=cart_db
      - MONGO_USER=${MONGO_USER}
      - MONGO_PASS=${MONGO_PASS}
      - SPRING_DATA_MONGODB_HOST=mongo
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - mongo

  # Payment Service
  payment-service:
    build:
      context: .
      dockerfile: ./payment-service/Dockerfile
    container_name: payment-service
    ports:
      - "8086:8086"
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - DB_URL=jdbc:postgresql://postgres-payment-db:5432/payment_service_db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - postgres-payment-db

  # [NEW] Delivery Service
  delivery-service:
    build:
      context: .
      dockerfile: ./delivery-service/Dockerfile
    container_name: delivery-service
    ports:
      - "8087:8087"
    environment:
      - EUREKA_SERVER_ADDRESS=http://service-discovery:8761/eureka
      - DB_URL=jdbc:postgresql://postgres-delivery-db:5432/delivery_service_db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - ZIPKIN_URL=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=docker
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,prometheus
    networks:
      - milhub-net
    depends_on:
      - service-discovery
      - postgres-delivery-db

  # 13. Redis
  redis:
    image: redis:7.2-alpine
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - milhub-net
    volumes:
      - redis-data:/data

  # 14. MongoDB
  mongo:
    image: mongo:7.0
    container_name: mongo
    ports:
      - "27017:27017"
    networks:
      - milhub-net
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - MONGO_INITDB_DATABASE=cart_db
    volumes:
      - mongo-data:/data/db

  # 15. Zipkin
  zipkin:
    image: openzipkin/zipkin:3
    container_name: zipkin
    ports:
      - "9411:9411"
    networks:
      - milhub-net

  minio:
    image: minio/minio:latest
    container_name: milhub-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - ./minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - milhub-net

  createbuckets:
    image: minio/mc
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c " echo 'Waiting for MinIO...'; until /usr/bin/mc alias set myminio http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD}; do
        echo '...MinIO is not ready yet, sleeping 1s...';
        sleep 1;
      done; echo 'MinIO is UP! Creating buckets...'; /usr/bin/mc mb --ignore-existing myminio/products; /usr/bin/mc anonymous set public myminio/products; /usr/bin/mc mb --ignore-existing myminio/avatars; /usr/bin/mc anonymous set public myminio/avatars; /usr/bin/mc mb --ignore-existing myminio/documents; /usr/bin/mc anonymous set private myminio/documents; echo 'All buckets created successfully!'; exit 0; "
    networks:
      - milhub-net

  # Nginx Reverse Proxy (Routes both Backend and Frontend)
  nginx:
    image: nginx:alpine
    container_name: milhub-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - milhub-net
    depends_on:
      - api-gateway

  ngrok-backend:
    image: ngrok/ngrok:latest
    container_name: ngrok-backend
    restart: unless-stopped
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command:
      - "http"
      - "--domain=rachell-diaphanometric-viva.ngrok-free.dev"
      - "nginx:80"
    networks:
      - milhub-net
    depends_on:
      - nginx
      
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - milhub-net

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    networks:
      - milhub-net
    depends_on:
      - prometheus

networks:
  milhub-net:
    driver: bridge

volumes:
  postgres-user-data:
  postgres-product-data:
  postgres-order-data:
  postgres-payment-data:
  postgres-delivery-data:
  redis-data:
  mongo-data:
