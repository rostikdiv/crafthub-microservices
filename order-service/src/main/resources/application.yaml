server:
  port: 8083 # ❗️ Новий порт

spring:
  application:
    name: order-service # ❗️ Важливо для Eureka та Feign

  datasource:
    driver-class-name: org.postgresql.Driver
    # ❗️ Нова, ізольована база даних
    url: jdbc:postgresql://localhost:5432/crafthub_order_db
    username: postgres
    password: 1111 # (твій пароль)

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format_sql: true

# Налаштування реєстрації в Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true