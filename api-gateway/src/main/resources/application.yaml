server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

          routes:
            - id: user_service_route
              uri: lb://user-service
              predicates:
                - Path=/api/v1/users/**, /api/v1/auth/**

            - id: product_service_route
              uri: lb://product-service
              predicates:
                - Path=/api/v1/products/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web.reactive: DEBUG

# -----------------------------------------------
# ❗️ Секція для JWT (виправлена)
# -----------------------------------------------
application:
  security:
    jwt:
      # ❗️ Має бути ІДЕНТИЧНИМ ключу в user-service
      secret-key: bXktc3VwZXItc2VjcmV0LWtleS1mb3ItY3JhZnRodWItbWljcm9zZXJ2aWNlcy1hbmQtZm9yLWp3dC1zaWduYXR1cmUtZ2VuZXJhdGlvbg==

      # ❗️ Доданий термін (для консистентності)
      expiration: 86400000