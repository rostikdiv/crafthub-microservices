<?xml version="1.0" encoding="UTF-8"?>
<!-- Logback configuration for Cart Service -->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Standardized console log pattern with trace/span ID support for distributed tracing -->
    <property name="CONSOLE_LOG_PATTERN" 
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) --- %green([%15.15thread]) %boldYellow([%X{traceId:-},%X{spanId:-}]) --- %cyan(%-40.40logger{39}) : %m%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Default log level is INFO -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>